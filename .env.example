# 复制本文件为 .env 并填入你的连接信息。
# .env 已被 .gitignore 忽略，不会被提交，适合放密码。
#
# MySQL 连接串格式: mysql://用户名:密码@主机:端口/库名
MYSQL_URL=mysql://root:password@127.0.0.1:3306/your_database

# Web 界面端口（macOS 的 5000 被 AirPlay 接收器占用，默认用 5050）
PORT=5050
